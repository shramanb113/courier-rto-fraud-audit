# Template only — never commit the real secret.
#
# Copy this to k8s/secret.yaml (gitignored), fill in a real password, and
# apply it before the other manifests:
#   cp k8s/secret.example.yaml k8s/secret.yaml
#   kubectl apply -f k8s/secret.yaml
#
# Or skip the file entirely and create it imperatively instead:
#   kubectl create secret generic rto-audit-db \
#     --from-literal=DATABASE_URL='postgresql+psycopg2://rto_audit:CHANGE_ME@rto-audit-postgres:5432/rto_audit'
apiVersion: v1
kind: Secret
metadata:
  name: rto-audit-db
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg2://rto_audit:CHANGE_ME@rto-audit-postgres:5432/rto_audit"
